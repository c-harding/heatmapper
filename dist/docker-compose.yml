version: '2'
services:
  heatmapper:
    restart: always
    build: .
    ports:
      - '${SERVER_PORT}:${SERVER_PORT}'
    volumes:
      - .env:/usr/src/app/dist/.env
      - ./sessions:/usr/src/app/dist/server/sessions
