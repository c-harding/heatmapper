version: '2'
services:
  heatmapper:
    restart: always
    build: .
    ports:
      - '${SERVER_PORT:-8080}:${SERVER_PORT:-8080}'
    volumes:
      - .env:/usr/src/app/dist/.env
      - ./sessions:/usr/src/app/dist/server/sessions
